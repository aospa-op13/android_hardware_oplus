android_app {
    name: "KeyHandler",

    srcs: ["src/**/*.kt"],

    certificate: "platform",
    platform_apis: true,
    system_ext_specific: true,

    static_libs: [
        "co.aospa.resources",
    ],

    optimize: {
        proguard_flags_files: ["proguard.flags"],
    },
}
